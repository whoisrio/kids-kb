# 2026-09-04

## 3-A Task 13 真实资料重入库验收（部分）

- 沙箱代理阻挡远程 Alibaba MaaS，VLM 改用本地 ollama qwen3.5:4b（`KB_VISION_BASE_URL=http://localhost:11434/v1` + `KB_DOC_OGNIZE_BASE_URL=...` 等）。
- docx 两份（语法一阶/二阶 期末测试）：之前已完成 ingest+检索+聊天引用，状态 `parsed`、1 chapter、3-4 chapter_chunks。
- PDF `26春1下数学北师大学霸提优大试卷.pdf`（72 页扫描卷）：
  - `uv run python -m kb.cli ingest` 成功，doc_id=`c37ffc18-fdaa-4144-9531-03e746268012`，状态 `rendered`，72 页全 `parsed`，每页 page_md 已有（block_type=page）。
  - `kb.cli structure c37ffc18...` 失败：`未找到目录页，请用 --toc-pages 显式指定`。
  - 这份"学霸提优大试卷"是试卷集合（封面分 素养达标6套/专项突破4套/...），没有标准 目录页 → TOC-driven `extract_toc` 不适用。chapters=0/chunks=0，approve 无 items 可批。
- 验收结论：PDF ingest 端到端通了（VLM local ollama 72 页识别成功），structure/approve 在"无 TOC 试卷集合"上阻塞。后续这类 PDF 要么改用"按 page 落章节 chunks"的简版路径，要么让用户在复核页手动选题（属 Phase 3-C 复核页迁移范畴）。
- 后续尝试：第 4 页就是"目录"（左半边 "目录" 标题 + "同步分层提优/素养达标第N套/专项突破/专项突破第N套" 等列表），但显式 `--toc-pages 4` 走 qwen3.5:4b VLM 输出的 TOC JSON 解析失败（`json.decoder.JSONDecodeError: Expecting value: line 1 column 1`）。本地 ollama 模型最大就 qwen3.5:4b，再大就需要换模型（沙箱环境限制）。
- 双重阻塞：① 4b 模型对 TOC 抽取 prompt 的 JSON 输出不稳；② "素养达标第N套" 不属于 pipeline `toc.py` TOC_PROMPT 期望的"第N讲"语义范围（pipeline 设计只适合练习册按讲分章）。这道验收实质上需要 Phase 3-C 复核页迁移或新 PDF 类型支持来收尾。

## 3-B Task 1 P0 backend source.pdf 回传

- `backend/src/routes/papers.ts` 新增 `GET /api/papers/:id/source.pdf`：从 `cfg.storageRoot/papers/<id>/source.pdf` 读盘回 `application/pdf`。非法 UUID 422（22P02），盘上缺失 404（ENOENT），未知错冒泡。
- 测试 `backend/src/routes/papers.test.ts` 加 "GET /:id/source.pdf 回传原件;缺失 404;非法 id 422"：RED（路由不存在 404）→ GREEN（6/6 passed）。
- 提交 `77f8565`：`feat(backend): GET /:id/source.pdf 回传试卷原件`。

## 3-B Task 1 done → next: Task 3 failed 原件预览（前端+backend）。

## 3-B Phase 全量完成

按计划连续推进 3-B 全部 15 项任务。提交按顺序（commit 短哈希）：

- `77f8565` Task 1 P0 backend `GET /:id/source.pdf` 回传。
- `41d7ca3` Task 2 P0 坏 PDF 先 fitz.open 验证再落盘（pipeline `paper_pipeline.py`）。
- `2b198e1` Task 3 P0 failed 详情页图 + source.pdf 内嵌 + 整卷入口。
- `76cff3c` Task 4 P1#1 非 UUID 422 全族统一（retry/re-recognize/pages-image/candidates/image）。
- `2a10312` Task 5 P1#2 subject 下推 SQL（vectorHits + BM25）+ matchQuestion topK=10。
  - 注：新测试并非严格 RED（post-filter 也行得通），仅作行为保险。
- `60b4b31` Task 6 P1#3 re-recognize 自动匹配限页（人工清除不重挂）。
- `512fc0d` Task 7 P1#4 坏 PDF 上传 422 带文件名 + 前端透传服务端 error 文案。
- `b3e801c` Task 8 P1#5 internal_api 每请求 `connect()` 后 `finally: conn.close()`。
- `c058523` Task 9 P1#6 队列孩子维度：过滤下拉 + 队列 child_name 徽标。
- `d7006ac` Task 10 P1#7 浮层键盘守卫（matchOpen/uploadOpen 拦快捷键）+ 确认失败 UI 反馈。
- `70e4971` Task 11 P1#8 storageRoot 改 `fileURLToPath(new URL(..., import.meta.url))`。
- `c137ac2` Task 12 P1#9 migration 0012 加 ctid 决胜去重（plan 代码 bug：`MIGRATIONS_DIR.parent / ".env"` → `parent.parent / ".env"`）。
- `483c37e` Task 13 P1#10 队列 failed 重试变真 button（paper-item 由 button→div role=button）。
- `bbad257` Task 14 E2E 坏 PDF 422 文案直达 UI（paper-pipeline.spec.ts）。
- `d42a70f` Task 15 backlog 清账（删除线 + commit 引用索引）。

测试通过情况：backend 16 文件 95/95 测试通过；frontend 单文件 11/11 通过；E2E 未实跑（环境未拉起 Playwright 全链路）。计划里的 plan-bug 修了 3 处（`AND i.chapter`、re-recognize UUID 提前校验、0012 ctid）。

## Phase 3 进展 Review（下午全面核实）

- **单测实测全绿**：pipeline 174 passed/1 skipped（basetemp 指到项目内绕开沙箱 EEXIST 干扰）、backend 16 文件 95/95、frontend 全量 9 文件 50/50（日志里"11/11"只是单文件）。
- **3-A**：13 个 Task，Task 1-12 提交全覆盖且 searchability E2E 实跑 1 passed（1.2m）；Task 13 部分完成（docx 通，无 TOC 试卷 PDF 阻塞，等 3-C）。
- **3-B**：15/15 提交覆盖；paper-pipeline E2E 首次实跑暴露两个问题。
- **E2E spec 回归（已修，未提交）**：3-B Task 9 队列页新增"孩子"过滤下拉后，页面上有两个 `aria-label="孩子"`，spec `getByLabel("孩子")` strict mode 撞车（t1 + 坏 PDF 两处）。修法：定位收拢到 `getByRole("dialog", { name: "上传试卷" })` 作用域内（e2e/specs/paper-pipeline.spec.ts）。
- **paper-pipeline t1 深层失败根因（产品外因）**：管线报"未识别出题目"。逐层排查：fixture/prompt/代码昨天后未变 → 远程 MaaS `qwen3.8-2.4t-a95b` 现在**只剩文本能力**，直接问图它自认"text-only language model cannot process visual content"，于是按 prompt 规则回空数组。昨天同一 fixture 同 prompt 能拆 3 题——模型端部署漂移，非代码回归。本机 ollama 最大 qwen3.5:4b，TOC JSON 都不稳，替代不了。待用户决策：换 MaaS 视觉模型部署 / 控制台核实。
- **其他 review 发现**：两份 phase3 计划文档 checkbox 一个没勾（工作做完了进度没回写）；分支领先 origin 28 个提交未推。
- 环境备忘：e2e 跑之前先 `mv test-results /tmp/...`（safe-delete 护栏拦 playwright 清目录）；沙箱内跑 playwright webServer 会 120s 超时假象，非沙箱跑正常；ps -E 看进程环境被 macOS 拦，探服务进程环境改走独立 CLI 复现。

## 换 VLM 模型后 E2E 全绿收尾（晚间）

- 用户把 `KB_VISION_MODEL` 换成 `qwen3.7-flash-2026-07-15`，fixture 三页独立探针全对（1 题各页，result=wrong/correct/None 符合设计）。
- **关键坑：serve-internal 的 `.env` 是进程启动时读的**，改完模型必须重启 pipeline 服务，否则常驻进程还用旧模型（llm_calls 计量表可以查服务实际用的模型，好用的取证点）。
- 从 WorkBuddy 会话跑 e2e 的三个环境坑（已摸清，下次直接照做）：
  1. 沙箱注入 `HTTP_PROXY=127.0.0.1:57187` 且无 NO_PROXY → Playwright 探活 localhost 被代理劫持回 502，误判服务没起、重复拉起撞 EADDRINUSE。解法：跑时加 `NO_PROXY=127.0.0.1,localhost`（远程 MaaS 仍走代理，两全）。
  2. 沙箱下 node `os.userInfo()` 解析不出用户 → pg Pool 连 `postgresql://localhost/kb`（无用户名）报 "no PostgreSQL user name specified in startup packet"。解法：加 `PGUSER=rio`。
  3. playwright webServer 120s 超时假象、safe-delete 拦 test-results 清理（跑前 mv 走）、`uv run pytest` 默认 basetemp EEXIST → `--basetemp=.tmp/pytest`。
- 最终结果：**paper-pipeline E2E 5 passed (1.6m)**（t1 VLM 拆题/预识别、t2 自动匹配、t3 键盘确认落 attempts、t4 改判、坏 PDF 422），加此前 searchability 1 passed——Phase 3 两个 E2E spec 全部实跑验证通过。
- spec 修复已提交 `b134cc3`（上传弹窗定位收拢到 dialog 作用域）。
- 单测基线：pipeline 174 passed/1 skipped、backend 95/95、frontend 50/50。
- 待办遗留：3-A Task 13 无 TOC 试卷 PDF（等 3-C）；分支领先 origin 29 提交未推；两份 phase3 计划 checkbox 未回写。
- checkbox 已回写（`8348f40`）：3-A 勾 Task 1-12 + Task 13 Step 1（共 64 项），Steps 2-5 留白并在 Task 13 下附阻塞说明；3-B 75 项全勾。
- **教训（我自己埋的雷）**：E2E 排查 kill 掉用户常驻 pipeline 服务后没重启，Playwright 临时实例跑完即回收，8766 空窗数小时 → 用户聊天检索连续报"题库检索失败"。已 nohup 重启（日志 /tmp/kb-pipeline.log，带新 VLM 配置）。**以后 kill 常驻服务必须当场拉回来**。注意该进程在 start.mjs 体系外，用户下次重启三服务前需先杀掉它，否则端口冲突。
## qwen3.5 思考饥饿排障（16:00-16:25，用户更新对话模型 qwen3.5:4b-32k 后）

用户更新了 ollama 对话模型（新增 qwen3.5:4b-32k），重开 3-A Task 13 的 PDF 验收。根因链全部查清：

1. **根因**：qwen3.5 系列默认开 thinking。TOC 抽取的 reasoning 烧 4.5k+ token，`transcribe_image` 的 `max_tokens=4096` 被吃穿 → content 返回空串。ollama 0.33.2 OpenAI 兼容层**不透传** `extra_body={"think":false}`（原生 /api/chat 认 think 参数）；`/no_think` 软开关对 qwen3.5 无效；**`reasoning_effort: "none"` 兼容层认**（实测秒回，reasoning 0）。
2. **更严重的发现**：72 页 ingest 时的页面转录**全是空串**（ingest 用 qwen3.5:4b 同样思考饥饿）——之前"72 页 parsed"是假象。且空串块逃逸了 run_parse 自愈检查（只查 IS NULL），页被误标 parsed。
3. **修复（3 个 commit，均 TDD）**：
   - `ffb8aae` transcribe_image max_tokens 4096→12288 + toc `normalize_toc_entries`（chapter_no "第一单元"→顺序重排 1..N，脏条目丢弃）。
   - `5594f58` 超大图降采样 ≤2000px 再送视觉模型（`_downscale_for_vision`，fitz 实现）。
   - `c28b222` 转录默认 `extra_body={"reasoning_effort":"none"}`，服务端拒绝时降级重试。
   - `4c1fede` 空转录判失败（parse_error='转录结果为空'），块保持 NULL 等重试。
4. **structure 结果**：25 章入库（试卷集合：单元 A/B 卷×6 + 专项 4 + 期末 8 + 参考答案），print_page 1..157（物理页 72 = 两页合一排版）。页码校准 0：块文本里没有章节标题（正文页转录当时全空），待重转录后重跑。
5. **重转录**：已重置 72 块 content_md=NULL、pages→rendered，后台 run_parse 重跑（关思考后单页应快很多）。之后 structure（extract_toc 幂等跳过，calibrate 重新校准）→ export → approve/embed → chat 验收。
6. **环境坑**：沙箱会 SIGKILL(137) 前台长驻 Python 进程（LLM 调用 >3-5min 必死），长任务必须 run_in_background；Bash 后台任务不继承 cwd，命令内必须显式 cd（多次踩坑后改用 wrapper 脚本 /tmp/kb-toc/run-*.sh）。start.mjs 起 backend 需要 PGUSER=rio，孤儿进程占 8766 端口需 lsof 清。

# 2026-09-03

## Phase 2 试卷管线（docs/superpowers/plans/2026-09-03-paper-pipeline.md）16 Task 全部完成

逐 Task 单 commit 完成（3d320e5 → 459f472），覆盖 pipeline 5 / backend 8 / frontend 2 / e2e 1。

### 本日完成部分
- Task 14 frontend api 层、Task 15 复核视图（ReviewView + PaperQueue/UploadDialog/QuestionCard/MatchPicker + Rail/App/theme.css 接线），全量 42 测试绿。
- Task 16 E2E：`e2e/fixtures/make_paper.py`（pymupdf 合成 3 张三年级数学卷图：红✗/✓/无痕迹）+ `e2e/specs/paper-pipeline.spec.ts` 4 串行用例，真库 kb + ollama bge-m3 + 远端 VLM 全链路，4 passed。

### E2E 暴露并修复的核心 bug：VLM 拆题 LaTeX 记号 vs 题库自然文本
- 现象：t2 自动匹配 matched_item_id 一直 null，检索链路本身正常（同文候选 vec_score=1.0）。
- 根因：VLM 拆题输出 `2. $135 \div 5 =$`（LaTeX + 序号 + $），题库是自然文本 `135 ÷ 5 =`；bge-m3 对两者余弦仅 0.75 < 0.88 阈值（还原成 `2. 135 ÷ 5 =` 后 0.97 命中）。
- 修复：`pipeline/kb/paper_pipeline.py` 新增 `_normalize_content_md`（\frac→(a)/(b)、\div→÷、\times→×、剥 $ 与行首序号、折叠空白，幂等），parse_page_questions 落库前应用；PAPER_VLM_PROMPT 措辞改"算式用自然文本"。测试补 LaTeX 归一化用例、更新落库断言（剥序号后 content_md 无 "1. " 前缀）。

### 排查经验
- backend 自动匹配 per-question try/catch 吞错 → 观察手段：给 jobs.ts 匹配循环临时加 console.error 诊断日志（tsx watch 自动重载），跑完删。实测日志空=循环正常但 auto 全 NULL，据此定位到文本归一化层而非检索层。
- e2e Playwright webServer stdout 重定向到文件时不可见，但手动 `npm run dev` 起的 backend 会被 reuseExistingServer 复用且日志实时可查。
- 沙箱 safe-delete 护栏：playwright 自己清 test-results 目录（>50 文件）会被拦 → 跑前 `mv test-results /tmp/...` 挪走即可，别 rm -rf。
- pipeline serve-internal 是旧代码（9:33 起，无 /internal/ingest-paper）→ kill 后同命令重启即加载新 internal 路由。

### 环境备注
- 真库 kb 的 schema_migrations 曾为空（表都是手工/历史建的）→ 回填 0001-0010 标记，migrate 只跑 0011。
- papers.child_id NOT NULL；paper_questions 唯一约束 (paper_id,page_no,seq_in_page)，无 seq 列。
- psycopg 非 autocommit 忘 COMMIT 会静默回滚（复现数据插了两次都不见，教训）。

## Phase 2 Code Review 核实（外部 review 意见逐条验证）

结论：C1+I1-I4 全部属实，Minor 多数属实，2 处归因/依据有出入。
- **C1 实测复现**：POST /internal/ingest-paper 不存在 id → HTTP 500 text/plain "Internal Server Error" 无 JSON detail。internal_api.py 两端点漏 try/except（plan Task5 L891-897 有 except→HTTPException(500,detail=str(e)) 被丢），jobs.ts `detail.detail ?? pipeline 500` 提取不到 → papers.error 落废话。plan 测试只断言 status 500 未断言 detail 内容。
- I1：attempts 无 UNIQUE(paper_question_id)（0011 注释自称"至多一条"但 schema 没兜），confirm SELECT-then-INSERT 竞态窗口真实。修：CREATE UNIQUE INDEX（pg NULL 不冲突，item 来源行不受影响）+ ON CONFLICT。
- I2：spec L124 总超时 10 分钟，jobs.ts 三处 fetch 无 AbortSignal，undici 300s 掐断。
- I3：spec L52/L122 bbox null=整页兜底，实际 bbox None → 不进 _crop_question → image_path NULL → 题图 404 → QuestionCard onError 藏图；pageImageUrl 全前端零调用。
- I4：index.ts serve 回调直跑 redriveStuckPapers 无探活，start.mjs 并行起服务竞态真实（触发需启动时恰有滞留 processing 卷）。
- 出入 1：Minor2 "spec 写 topK=10" 实为 design spec L134=10 vs plan Task8=5 文档打架，实现跟了 plan。
- 出入 2：Minor6 后端列表 GET 已支持 child_id query、前端 fetchPapers(childId?) 可选参——缺的是 ReviewView 没传参/队列不显示孩子名，属 UI 未接 + 超 spec 的 enhancement。
- 出入 3：Minor7 队列 failed "重试" 是 span 哑装饰，但 ReviewView 详情失败框有真 retryPaper 入口。
- 遗留待修 backlog：C1 detail 透传 + I1 唯一约束 + I2 超时 + I3 题图兜底 + I4 探活（未动手，等用户拍板）。

## Code Review 五项修复完成（2615347→8f18e2a，各单 commit）

用户 go 后按 C1→I1→I2→I4→I3 顺序逐项修完，每项单测+回归+单 commit：
- C1 2615347：internal_api.py ingest_paper_ep/recognize_page_ep 包 try/except→HTTPException(500,detail=str(e))；monkeypatch 已知异常断言 detail 精确透传（注意：函数内 `from kb.paper_pipeline import ingest_paper` 每次调用现取模块属性，monkeypatch 模块属性即生效）。
- I1 6b133fa：migration 0012（先 DELETE 历史重复再 CREATE UNIQUE INDEX attempts_paper_question_id_key，pg UNIQUE 允许多 NULL，item 来源行不受影响）；confirm 改 INSERT...ON CONFLICT(paper_question_id) DO UPDATE 单语句；真库 kb 已 migrate 应用 0012（.env key 是 KB_DATABASE_URL 不是 DATABASE_URL）。
- I2 8aaabb8：jobs.ts 三处 fetch 挂 AbortSignal.timeout(600_000)。
- I4 d798bf2：新增 redriveWhenPipelineReady（/internal/rerank 空 docs 探活——docs 空时不加载模型恒 200，好探活点；默认 30 次×2s 退避），serve 回调改调它；顺带修 jobs.test 既有竞态：redrive 是 fire-and-forget(void drivePaper)，断言前要轮询等卷脱离 processing，否则 afterAll pool.end() 后 job 仍访问 pool。
- I3 8f18e2a：QuestionCard 三态（crop→page→missing）+ useEffect [question.id] 重置，不依赖调用方 key；顺带修既有 papers.test.ts TS6133 未用参数（tsc -b gate 拦路石）。

回归：pipeline 157 passed / backend 80 passed / frontend 44 passed / e2e paper-pipeline 4 passed（54s）。
教训：vitest 多测试同 describe 共享真库，fire-and-forget 的 job（redrive void drivePaper）会跨测试泄漏，afterAll pool.end 后报 "Cannot use a pool after calling end"——测试里必须等 job 落定。
